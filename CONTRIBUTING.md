# 기여 가이드

맞춤법 규칙 추가는 가장 환영하는 기여입니다. **C# 지식 없이 JSON 한 덩어리만 추가하면 됩니다.**

## 규칙 하나 추가하는 법 (3단계)

### 1. 알맞은 파일 열기

`src/HangulNotifier.Core/Rules/` 안에서 범주에 맞는 파일을 고릅니다.

| 파일 | 무엇을 담나 |
|---|---|
| `doe-dwae.json` | 되/돼, 봬/뵈 관련 |
| `impossible-syllables.json` | 됬·됀처럼 한국어에 없는 음절 |
| `standard-words.json` | 역할·어이없다처럼 단어 자체 오표기 |
| `loanwords.json` | 메시지·초콜릿 등 외래어 표기 |
| `sai-siot.json` | 등굣길·최댓값 등 사이시옷/모음 |
| `endings-colloquial.json` | -ㄹ게, -구요 등 어미·구어체 |
| `context-info.json` | 문맥 없이는 판단 불가한 참고 항목 |

### 2. 규칙 추가하기

배열에 객체 하나를 추가합니다. VS Code로 열면 **자동완성과 실시간 검증**이 됩니다.

```json
{
  "id": "meseji",
  "pattern": "메세지",
  "suggestion": "메시지",
  "message": "'메시지'가 바른 외래어 표기입니다.",
  "level": "Certain",
  "examples": ["메세지", "메세지를"],
  "okExamples": ["메시지", "메신저"]
}
```

| 필드 | 설명 |
|---|---|
| `id` | 전역 유일. 소문자 로마자+하이픈. |
| `pattern` | 어절에 적용할 **정규식**. 어절 끝만 매치하려면 `$`(예: `되요$`). |
| `suggestion` | 올바른 표기. 알림에 크게 표시됩니다. |
| `message` | 왜 틀렸는지 한 줄 설명. 근거를 담아 주세요. |
| `level` | `Certain` / `Suspect` / `Info` (아래 참조) |
| `examples` | **반드시 감지돼야 하는** 오류 표기들. `previousWordPattern`이 없는 규칙은 필수. |
| `okExamples` | **절대 감지되면 안 되는** 올바른 표기들. **모든 규칙 필수** — 최소 1건. |

`examples`/`okExamples`가 곧 테스트입니다. **별도 테스트 코드를 쓸 필요가 없습니다.**

`previousWordPattern`(직전 어절 조건)이 있는 규칙은 `examples`에 `"직전어절 어절"`처럼
**공백으로 구분**해 적으면 됩니다. 마지막 공백을 기준으로 앞은 `previousWord`, 뒤는 검사할
어절로 나뉘어 테스트됩니다. (예: `an-dwae`의 `"안 되"` → `previousWord="안"`, `word="되"`)

### 3. 검증하기

```bash
dotnet test
```

이 한 줄이 전부입니다. 통과하면 PR을 올려 주세요.

## 신뢰도(level) 고르기

**오탐 제로가 이 프로젝트의 최우선 원칙입니다.** 알림이 잘못 뜨는 것은 안 뜨는 것보다 나쁩니다.

| level | 기준 | 예 |
|---|---|---|
| `Certain` | 문맥과 무관하게 **항상** 틀린 표기 | `됬`, `메세지`, `역활` |
| `Suspect` | 문맥에 따라 맞을 수도 있음 | 문장 끝의 `되` |
| `Info` | 실제 단어와 충돌 가능(기본 OFF) | `돼지` |

**애매하면 한 단계 낮추세요.** 동음이의어(회수/횟수)나 문맥 의존 표현(낳다/낫다)은 아예 넣지 않는 편이 낫습니다.

## 테스트가 실패했다면

실패 메시지가 무엇이 잘못됐는지 정확히 알려줍니다.

- `'<단어>'(코퍼스: "...") → 규칙 '<id>'에 오탐`
  → 정답인 단어가 당신의 규칙에 걸렸습니다. **패턴을 좁히세요**(예: `$` 추가). 코퍼스에서 그 문장을 지워서 통과시키면 안 됩니다.
- `규칙 '<id>'의 예시 '<단어>'은(는) 감지돼야 합니다`
  → `examples`가 규칙에 안 걸립니다. 패턴을 확인하세요.

## 개인 규칙만 쓰고 싶다면

PR 없이 본인 PC에서만 쓰려면 `%APPDATA%\HangulNotifier\user-rules.json`에 같은 형식으로 넣으면 됩니다. 앱 재시작 시 적용되며, 같은 `id`는 내장 규칙을 덮어씁니다.

## PR 규칙

- 커밋 메시지 본문은 한글, prefix는 영어(`feat:`, `fix:`, `docs:`).
- `HangulNotifier.Core`는 순수 유지 — Win32/WPF 코드를 넣지 마세요.
- 규칙 추가 PR은 `dotnet test`가 그린이면 충분합니다.
